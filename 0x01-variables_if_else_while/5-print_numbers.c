#include <stdio.h>

/**
  * main - Prints all the base numbers from 0 -9.
  *
  * Return: 0 to terminate the program.
  */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("Number %d\n", n);
	}

	return (0);
}
